import React from 'react'

export default function ClearCompleteBtn({learall}) {
  return (
    <div>
            <button className="button" onClick={learall}>Clear completed</button>
    </div>
  )
}
